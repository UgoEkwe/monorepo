{
  "name": "{{PACKAGE_NAME}}",
  "version": "{{VERSION}}",
  "description": "{{DESCRIPTION}}",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "python main.py",
    "build": "tsc && python -m py_compile *.py",
    "start": "python main.py",
    "type-check": "tsc --noEmit",
    "test": "python -m pytest tests/ && vitest run",
    "test:watch": "vitest",
    "install:python": "pip install -r requirements.txt",
    "install:js": "npm install"
  },
  "dependencies": {
    "@modular-ai-scaffold/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.8"
  }
}